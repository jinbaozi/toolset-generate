#!/usr/bin/bash
# gts-agent 生成的 gcc-toolset-__GTS_ID__-env wrapper（方案 20.5）。
set -euo pipefail

source "/opt/rh/gcc-toolset-__GTS_ID__/enable"

if (($# == 0)); then
    exec "${SHELL:-/bin/bash}"
elif (($# == 1)); then
    exec /bin/sh -c "$1"
else
    exec "$@"
fi
