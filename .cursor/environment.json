{
  "name": "gts-agent",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3-venv build-essential binutils && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -e '.[dev]'"
}
